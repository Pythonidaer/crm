DATABASE_URL=

# Optional: enable Postgres-backed leads in the UI (requires local API or production backend)
VITE_USE_DATABASE_LEADS=false

# Optional: override API origin when not using Vite dev proxy (production backend URL)
# VITE_API_BASE_URL=

# Local API port for `pnpm dev:api` (default 3001)
# API_PORT=3001
